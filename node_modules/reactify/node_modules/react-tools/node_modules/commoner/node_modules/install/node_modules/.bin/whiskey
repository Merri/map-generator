#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../whiskey/bin/whiskey" "$@"
  ret=$?
else 
  node  "$basedir/../whiskey/bin/whiskey" "$@"
  ret=$?
fi
exit $ret
